---
description: 把一句话沉淀为长期记忆（偏好 / 事实 / 纠正 / 流程）
---

调用 `mcp__proactive-agent__memory_capture` 把以下内容写入长期记忆，type 按语义选择（preference / fact / correction / sop / todo_context）：

$ARGUMENTS

写入时保持原意完整，**否定词必须保留**（"不要用 X" 必须原样记录）。
